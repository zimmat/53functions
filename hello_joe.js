module.exports = function(name){
    // this.hello_joe = function(userName){
      this.userName = name;
      if(name == "Xolani"){
      return "Hello," + " " + name.toUpperCase() + "!";
    }
      else  if(name == "Joe" || "Bob"){
          return "Hello!";
        }
    // }

  };
